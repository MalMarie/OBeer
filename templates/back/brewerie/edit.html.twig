{% extends 'back/base_back.html.twig' %}

{% block title %}Modifier {{ brewerie.name }}{% endblock %}

{% block body %}
    <h1>Modifier {{ brewerie.name }}</h1>
  
    {# avec l'include de Twig, on peut transmettre des arguments #}
    {{ include('back/brewerie/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    
    <div class="d-flex justify-content-start">

        <a href="{{ path('app_back_brewerie_index') }}">
            <button class="btn btn-info m-2">Retour à la liste</button>
        </a>

        {{ include('back/brewerie/_delete_form.html.twig') }}

    </div>
{% endblock %}
